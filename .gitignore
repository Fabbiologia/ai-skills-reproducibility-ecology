# The manuscript itself is not archived here. This repository holds the study:
# the tasks, the data, the run records, the analysis and the figures. The Word
# documents and the scripts that build them live alongside it but are not part
# of the archive, and remain in the history of this repository up to d58d176.
manuscript/
verify_documents.py
npm-debug.log*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Build/preview artifacts
manuscript/_preview/
manuscript/*.pdf

# OS
.DS_Store
Thumbs.db

# Private working notes (author only, never published)
private/

# Restricted data: the real LTEM reef-fish monitoring extract is NOT distributed
# in this repository. It is available on request from the Aburto Lab.
# See data/ltem_cabo_pulmo_2023.csv.README.md.
data/ltem_cabo_pulmo_2023.csv

# Word lock/temp files
~$*
transfer_runs/data/
