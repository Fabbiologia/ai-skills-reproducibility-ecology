# Tested versions for the deterministic reproduction (analysis + figures).
# The three core scientific libraries are pinned to the versions used in the paper;
# plotting/data helpers are given as lower bounds.
numpy==2.4.6
scipy==1.16.3
scikit-learn==1.9.0
matplotlib>=3.8
pandas>=2.0
jsonschema>=4.21
